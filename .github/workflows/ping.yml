name: Ping market gateway

on:
   workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Warm multi endpoint
        run: |
          curl -sS "https://crypto-market-gateway.vercel.app/api/multi?driver_tf=5m" > /dev/null
